export declare type Endpoints = {
    api: string;
    lobby: string;
    user: string;
};
/**
 * @internal
 */
export declare const setup: {
    debug: boolean;
    endpoints: {
        api: string;
        lobby: string;
        user: string;
    };
};
