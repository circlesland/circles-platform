import type {Token} from "./token";
import type {Addressable} from "./addressable";

/**
 * Represents a gnosis safe that participates in the circles network.
 */
export interface Safe extends Addressable
{
}
